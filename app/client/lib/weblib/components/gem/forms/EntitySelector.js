BASE.require([
    "jQuery",
    "BASE.async.Fulfillment"
], function () {

    BASE.namespace("components.gem.forms");

    var Future = BASE.async.Future;
    var Fulfillment = BASE.async.Fulfillment;

    components.gem.forms.EntitySelector = function (elem, tags, services) {
        var self = this;
        var $elem = $(elem);

        
    };

});